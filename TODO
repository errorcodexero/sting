-Laptop configuration
	-ssh config:
		-add "StrictHostKeyChecking no/UserKnownHostsFile=/dev/null" for robot
-Try out the new Talon SRX's
	-CAN
		-just power levels
		-try limit switch feedback
		-try encoder feedback
		-sensor-based closed-loop modes
-Vision stuff
-Autonomous mode stuff-Logan&Matthew
-Drivebase code
	-make it so that there isn't such a deadband around 0 for fwd/backward (like 1/2 of joystick travel)
	-put in nudge buttons
-Lifter Code
	-UI
	-Motor and Encoders
	-Gripper 
	-Lifter Simulator-Jared
	-figure out the limits of when we throw totes
-Write logging code
-OI:
	-put in outputs
		-into framework
		-figure out if need to reprogram the TI launchpad
-Cleanup stuff:
	-use enum class more places
	-use std::array in place of C arrays in several places
-Try out serial console cable
